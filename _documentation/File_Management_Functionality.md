# File Management Functionality

### On App First Run
1. Ensure that the stash directory exists - set `STASH_EXISTS=true`
1. If the stash directory has no model files, transfer all model files to the stash directory. 
1. Copy any missing JSON files to the stast tree 
1. ensure the SQLite3 dataqbase exists - `drawthing_companion.sqlite`

